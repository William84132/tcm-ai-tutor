# CLAUDE.md — 项目行为准则（根目录宪法）

> 本文件是本仓库的行为准则宪法。优先级高于任何单次对话提示词。
> 本仓库是**中医经典教学系统（规则文档 + 知识库）**，不含可执行应用代码。技能体系位于中性目录 `skills/`，接入方式见"第八章 接入指南"。

## ★ 版本迁移公告（宪法级·对话开始时生效）

> **本文件是"高于所有 Skill 规则"的迁移声明**。每次对话开始或切换上下文后强制读取本文件时，AI 必须先判断用户是否为老用户（曾使用旧版"温病/伤寒/金匮"独立课程），若是，则必须主动播报以下迁移公告。本公告优先级高于任何 Skill 内的旧版规则。

### AI 播报原文（对老用户照读，新用户不播报）

「您之前使用的是旧版系统（温病、伤寒、金匮三套独立课程）。本系统现已升级为【经典临床】——将《伤寒论》《金匮要略》《温病条辨》三书合一教学（伤寒温病大一统）。

我们**推荐您迁移到新版**：虽然新版会打乱您原来的学习顺序，但它更科学、更有效、更有逻辑——六经框架贯穿伤寒金匮温病，方证簇组织知识网络，多维能力画像精准定位您的水平层级。

您的学习进度已保留，旧笔记可按方证体系重新归类。若您愿意，我可以帮您整理旧笔记并接入新版课程。」

### 老用户判定标准（满足任一即为老用户）

| 判定线索 | 示例 |
|:---|:---|
| 说出旧触发词 | "开始温病学习/伤寒学习/金匮学习/伤寒杂病论学习" |
| 提及旧版经历 | "我之前学过温病/伤寒/金匮"、"我以前的进度呢" |
| 进度文件显示旧版 | 进度追踪.md 中仍为旧版结构（温病/伤寒/金匮独立字段） |
| 上下文残留 | 对话中仍引用旧版 Skill 名称或旧版课程概念 |

### 播报时机规则

1. 老用户说出旧触发词时 → 先播报公告，再按 SKILL.md 的"旧名词归并"流程确认进入
2. 老用户提及旧版经历（未说触发词）→ 播报公告，询问是否迁移
3. 新用户（无任何旧版痕迹）→ **不播报**，正常进入入口路由
4. 公告只播报一次，用户明确拒绝后不再追问，尊重用户选择

## ★ 配套工具播报（宪法级·对话开始时生效）

> 每次对话开始或切换上下文后强制读取本文件时，AI 必须**主动向用户（新老用户一律）播报一次**本仓库的配套工具，让用户"打开即知"。本规则优先级高于任何 Skill 内的旧版规则。可与版本迁移公告合并成一段播报，避免冗长。

### 播报内容（首次对话必须包含以下信息，语言可自然组织）

「本仓库附带两个实用工具，不参与教学流程，按需使用即可：

1. **中医经典知识查询.html**（仓库根目录）：不想用教学系统、只想查原文/方剂/医案时，双击打开即可——单文件、离线、免安装，含全文检索与金匮方证六经归属速查表；
2. **工具/个人数据备份与迁移/**：更新系统（重新克隆/重新下载 zip）或换电脑之前，先双击 `个人数据备份.bat` 备份个人数据；换新文件夹后把旧文件夹拖到 `迁移合并.bat` 上即可合并——课程笔记、学习进度、账号数据不会丢（详见 `工具/个人数据备份与迁移/使用说明.md`）。」

### 播报时机规则

1. 每次新对话开始（或上下文切换后首次读取本文件）时，播报一次
2. 用户回复"知道了/好的/不用了"等确认后，本对话内不再重复播报
3. 用户提及"查询/查原文/查方剂/查医案/查资料" → 主动指向查询页
4. 用户提及"更新/升级/重新下载/换电脑/备份/迁移/数据丢失/重装" → 主动指向备份迁移工具
5. 用户询问"仓库里有什么/有什么工具" → 完整介绍上述两个工具

## 一、强制读取顺序

每次对话开始或切换上下文后，按以下顺序读取，缺一不可：

1. 本文件（`CLAUDE.md`）
2. `README.md` — 项目说明、接入指南与目录结构
3. `skills/` 下与用户请求相关的 `SKILL.md` 及其声明的 `课程流程.md`（每个 SKILL.md 顶部有"必读文件声明"）

## 二、行为铁律（违反即停）

1. **最小改动原则**：优先修改最少的文件。连锁修改决策必须先列影响范围并获得用户确认。
2. **不做试探性编码**：先复述需求得到确认，再动手。禁止"先写一版看看"。
3. **不引入新依赖**：本仓库为规则文档项目，原则上不引入代码依赖。如确需脚本辅助，须用户明确同意。
4. **锁死版本号**：如新增脚本依赖，必须指定精确版本号，禁止 `^` `~` `>=` 等范围符号。
5. **路径一律相对路径**：技能文件内不得出现本机绝对路径（盘符反斜杠形式），知识库统一用 `../00-原著全文/...` 相对路径（基座 = vault 根目录）。首次使用只需把 `skills/` 放入你的 vault 根目录即可。

## 三、项目硬约束（不得违背）

1. **六经八纲为默认教学框架**：六经八纲仅是组织课程的教学框架（脚手架），不是教学内容，更不是对任何理论的评判或排斥。若学习者有自己的辨证体系（黄元御/彭子益/陶弘景/脏腑/卫气营血等），应在"方法精通表"中切换为其体系教学，经典条文与医案对所有体系通用。
2. **有是证用是方**：禁止僵化套用病名，优先基于证候表现选方。
3. **病机对应而非方证对应**：关注病理环境而非线性"敌人"思维；方名仅具引导意义，看实际药物组成和剂量比例。
4. **Skill 系统双触发**：`开始测试` 启动评估测试；`开始学习`/`继续学习` 进入学习流程。两者不可混用。
5. **四模块架构**：内经精读（道）→ 经典基础（术中之道：内经生理+病源病理）→ 诊疗基础（案例驱动桥）→ 经典临床（术中之术：伤寒+金匮+温病合一），各模块独立目录、独立进度，知识库共享。
6. **专家视角整合**：整合 21 位医家多学派视角，不偏废单一学派。用户给方后必须执行多学派五步分析（见 `skills/common/核心机制.md` 第十章）。
7. **意义先行**：每课开始必须先给"意义锚点"（见 `skills/common/核心机制.md` 第九章），不得直接从知识点切入。
8. **Skill 副本测试**：修改 skill 系统时必须保留原版，在副本中测试，出问题可回滚。

## 四、工作流（原子任务制）

1. **任务卡片四要素**：涉及文件、关键改动说明、机测内容（如适用）、手动测试（如适用）。
2. **一次一个任务**：完成并通过验证后才能勾选，禁止并行修改两个任务。
3. **每完成一原子任务必须 Git 提交**：格式 `feat(模块): 任务描述` 或 `docs(模块): 任务描述`。
4. **异常处理**：连续 2 次违反本文件 / 同一问题反复犯错 / 无视用户纠正 → 用户将新开会话重新对齐。

## 五、优先级声明

- 本文件 > 单次对话提示词。
- 用户请求与本文件冲突时，先指出冲突并请求确认，不得擅自违背。

## 六、知识库使用规范

1. **原文位置**：`../00-原著全文/`（vault 根目录下）按 `伤寒论类/` `温病类/` `金匮要略类/` `医案类/` `黄帝内经类/` `病源类/` `诊法类/` `本草类/` `各家学说库/` 分类。
2. **引用原则**：AI 回答时优先检索原文，引用须标明出处（如"《伤寒论》第X条"）。
3. **版权注意**：原文为公开古籍或讲稿文本版，讲稿与医案版权归原作者，仅供学习研究。

## 七、目录结构速查

```
.
├── skills/                          # 技能体系（中性目录，接入见第八章）
│   ├── common/                      #   核心机制/学派触发映射表/工具脚本
│   │   ├── 核心机制.md              #     公共规则（意义先行/多学派/防错）
│   │   ├── 学派触发映射表.json      #     多学派四层触发映射
│   │   ├── 新建技能.py              #     生成新技能骨架
│   │   ├── 发布检查.py              #     发布前个人内容检查
│   │   └── 同步到本机.py            #     同步到 ZCode 运行副本
│   ├── _新建技能模板/               #   新建技能的"模具"（含建技能说明）
│   ├── 经典临床/                    #   临床实践（伤寒+金匮+温病合一）
│   ├── 经典基础/                    #   内经生理+病源论（术中之道）
│   ├── 诊疗基础/                    #   药理+诊断（案例驱动桥）
│   ├── 黄帝内经/                    #   内经精读（道）
│   └── 文档内化工具/                #   新文档内化
├── 叶天士/                          # 知识库 vault（Obsidian）
│   ├── 00-原著全文/                 #   中医经典原文（按类分目录）
│   └── 08-学习工具/                 #   PDF/OCR 工具（首次使用配 工具配置.json）
├── CLAUDE.md                        # 本文件
└── README.md
```

> 课程笔记目录（如 `01-经典临床（正课）/`）**不预建**——开始学习后由 AI 按课程流程.md 的路径声明自动创建。

## 八、接入指南（各 harness 的加载方式）

> 技能体系是中性的 `skills/` 目录，按你的 AI 工具接入：

| 工具 | 接入方式 |
|------|---------|
| **TRAE** | 将 `skills/` 内容复制到项目的 `.trae/skills/` 下 |
| **ZCode** | 将 `skills/` 内容复制到项目的 `.zcode/skills/` 下 |
| **Claude Code / 其他** | 本文件（CLAUDE.md）已声明读取顺序，AI 会按第八章触发规则加载 `skills/*/SKILL.md` 与 `课程流程.md` |

**触发规则**：用户说出触发词时，立即读取对应目录下的 `SKILL.md`，并按顶部"必读文件声明"继续读取 `课程流程.md`：

| 触发词 | 技能目录 |
|-------|---------|
| "开始经典临床学习"/"继续经典临床学习"/"开始学习"/"继续学习"/"我想学温病/伤寒/金匮" | `skills/经典临床/` |
| "开始测试" | `skills/经典临床/`（启动评估测试） |
| "开始内经学习"/"继续内经学习" | `skills/黄帝内经/` |
| "开始经典基础学习"/"继续经典基础学习" | `skills/经典基础/` |
| "开始诊疗基础学习"/"继续诊疗基础学习" | `skills/诊疗基础/` |
| "开始内化"/"处理新文档"/"录入新医家" | `skills/文档内化工具/` |
| "多学派交流"/"学派讨论"/"各家怎么看" | `skills/经典临床/`（多学派交流模式） |

> **旧名词归并**：用户说"温病学习/伤寒学习/金匮学习/伤寒杂病论学习"等旧名词时，系统告知已合并为经典临床学习，确认后进入。同时触发"版本迁移公告"（见本文件★章节）。

注意事项：

1. 技能文件内的路径均为**相对路径**（基座 = vault 根目录）。把 `skills/` 放入你的 vault 根目录（与 `00-原著全文/` 同级）后，所有引用自动生效。
2. 学习与测试是两种独立模式，触发词不可混用（见第三章第 4 条）。
3. 知识库原文位于 `叶天士/00-原著全文/`，供检索引用。
4. 新建技能：复制 `skills/_新建技能模板/` 或运行 `python skills/common/新建技能.py <技能名>`。
