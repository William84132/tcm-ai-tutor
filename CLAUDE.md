# CLAUDE.md — 项目行为准则（根目录宪法）

> 本文件是本仓库的行为准则宪法。优先级高于任何单次对话提示词。
> 本仓库是**规则文档与知识库**，不含可执行应用代码。TRAE 自动识别 `.trae/skills/*/SKILL.md` 激活 Skill；其他 Agent 按"第八章 Skill 触发规则"手动加载。

## ★ 版本迁移公告（宪法级·对话开始时生效）

> **本文件是"高于所有 Skill 规则"的迁移声明**。每次对话开始或切换上下文后强制读取本文件时，AI 必须先判断用户是否为老用户（曾使用旧版"温病/伤寒/金匮"独立课程），若是，则必须主动播报以下迁移公告。本公告优先级高于任何 Skill 内的旧版规则。

### AI 播报原文（对老用户照读，新用户不播报）

「您之前使用的是旧版系统（温病、伤寒、金匮三套独立课程）。本系统现已升级为【经典临床】——将《伤寒论》《金匮要略》《温病条辨》三书合一教学（伤寒温病大一统）。

我们**推荐您迁移到新版**：虽然新版会打乱您原来的学习顺序，但它更科学、更有效、更有逻辑——六经框架贯穿伤寒金匮温病，方证簇组织知识网络，多维能力画像精准定位您的水平层级。

您的学习进度已保留，旧笔记可按方证体系重新归类。若您愿意，我可以帮您整理旧笔记并接入新版课程。」

### 老用户判定标准（满足任一即为老用户）

| 判定线索 | 示例 |
|:---|:---|
| 说出旧触发词 | "开始温病学习/伤寒学习/金匮学习/伤寒杂病论学习" |
| 提及旧版经历 | "我之前学过温病/伤寒/金匮"、"我以前的进度呢" |
| 进度文件显示旧版 | 进度追踪.md 中仍为旧版结构（温病/伤寒/金匮独立字段） |
| 上下文残留 | 对话中仍引用旧版 Skill 名称或旧版课程概念 |

### 播报时机规则

1. 老用户说出旧触发词时 → 先播报公告，再按 SKILL.md 的"旧名词归并"流程确认进入
2. 老用户提及旧版经历（未说触发词）→ 播报公告，询问是否迁移
3. 新用户（无任何旧版痕迹）→ **不播报**，正常进入入口路由
4. 公告只播报一次，用户明确拒绝后不再追问，尊重用户选择


## 一、强制读取顺序

每次对话开始或切换上下文后，按以下顺序读取，缺一不可：

1. 本文件（`CLAUDE.md`）
2. `README.md` — 项目说明、目录结构与使用方式
3. `.trae/skills/` 下与用户请求相关的 `SKILL.md` 与 `agent_prompt.md`

## 二、行为铁律（违反即停）

1. **最小改动原则**：优先修改最少的文件。连锁修改决策必须先列影响范围并获得用户确认。
2. **不做试探性编码**：先复述需求得到确认，再动手。禁止"先写一版看看"。
3. **不引入新依赖**：本仓库为规则文档项目，原则上不引入代码依赖。如确需脚本辅助，须用户明确同意。
4. **锁死版本号**：如新增脚本依赖，必须指定精确版本号，禁止 `^` `~` `>=` 等范围符号。
5. **路径示例保留**：Skill 文件中的本地绝对路径（如 `e:\叶天士\叶天士\`）为示例，须保留并在显著位置标注"请替换为本地实际路径"。

## 三、项目硬约束（不得违背）

1. **六经八纲为默认教学框架**：六经八纲仅是组织课程的教学框架（脚手架），不是教学内容，更不是对任何理论的评判或排斥。若学习者有自己的辨证体系（黄元御/彭子益/陶弘景/脏腑/卫气营血等），应在"方法精通表"中切换为其体系教学，经典条文与医案对所有体系通用。
2. **有是证用是方**：禁止僵化套用病名，优先基于证候表现选方。
3. **病机对应而非方证对应**：关注病理环境而非线性"敌人"思维；方名仅具引导意义，看实际药物组成和剂量比例。
4. **Skill 系统双触发**：`开始测试` 启动 4 模块渐进评估；`开始学习`/`继续学习` 进入学习流程。两者不可混用。
5. **三模块架构**：内经精读（道）→ 经典基础（术中之道：内经生理+病源病理）→ 经典临床（术中之术：伤寒+金匮+温病合一），三模块独立目录、独立进度，知识库共享。温病和伤寒杂病论的独立 Skill 已删除，合并为经典临床单一课程。经典基础新增为独立模块，入口三选一。
6. **专家视角整合**：整合 21 位医家多学派视角，不偏废单一学派。
7. **Skill 副本测试**：修改 skill 系统时必须保留原版，在副本中测试，出问题可回滚。

## 四、工作流（原子任务制）

1. **任务卡片四要素**：涉及文件、关键改动说明、机测内容（如适用）、手动测试（如适用）。
2. **一次一个任务**：完成并通过验证后才能勾选，禁止并行修改两个任务。
3. **每完成一原子任务必须 Git 提交**：格式 `feat(模块): 任务描述` 或 `docs(模块): 任务描述`。
4. **异常处理**：连续 2 次违反本文件 / 同一问题反复犯错 / 无视用户纠正 → 用户将新开会话重新对齐。

## 五、优先级声明

- 本文件 > 单次对话提示词。
- 用户请求与本文件冲突时，先指出冲突并请求确认，不得擅自违背。

## 六、知识库使用规范

1. **原文位置**：`叶天士/00-原著全文/` 下按 `伤寒论类/` `温病类/` `金匮要略类/` `医案类/` `黄帝内经类/` `病源类/` `各家学说库/` `杂类/` 分类。
2. **引用原则**：AI 回答时优先检索原文，引用须标明出处（如"《伤寒论》第X条"）。
3. **版权注意**：原文为公开古籍或讲稿文本版，讲稿与医案版权归原作者，仅供学习研究。

## 七、目录结构速查

```
.
├── .trae/skills/                   # TRAE 技能规则（核心）
│   ├── common/                     #   三模块共享配置
│   ├── 中医经典临床学习导师/         #   经典临床学习（伤寒+金匮+温病合一）
│   ├── 中医黄帝内经学习导师/         #   内经精读学习（道）
│   ├── 中医经典基础学习导师/         #   经典基础学习（术中之道）
│   ├── 文档内化工具/                 #   新文档内化工具
│   ├── AI编程行为准则/              #   AI 编程规范
│   ├── 全自动PDF处理/               #   PDF 处理技能
│   └── 扫描版PDF处理/               #   扫描版 PDF OCR 技能
├── 叶天士/                          # 原始资料库（Obsidian vault）
│   ├── 00-原著全文/                 #   中医经典原文
│   ├── 08-prompt/
│   └── 09-医案学习数据库/
├── CLAUDE.md                       # 本文件
└── README.md
```

## 八、Skill 触发规则（供非 TRAE Agent 参考）

> TRAE 会自动识别 `.trae/skills/*/SKILL.md` 并按关键词激活 Skill；其他 Agent（Claude Code、CodeX 等）需按本章节手动加载规则文件。

当用户说出触发词时，**立即读取对应目录下的 `agent_prompt.md`**，并严格按照该文件的规则与用户交互：

| 触发词 | 规则文件 | 说明 |
|-------|---------|------|
| "开始经典临床学习" / "继续经典临床学习" / "开始学习" / "继续学习" | `.trae/skills/中医经典临床学习导师/agent_prompt.md` | 进入经典临床学习流程 |
| "开始测试" | 同上 | 启动 4 模块渐进评估 |
| "开始内经学习" / "继续内经学习" | `.trae/skills/中医黄帝内经学习导师/agent_prompt.md` | 进入内经精读学习模式（道，无测试） |
| "开始经典基础学习" / "继续经典基础学习" | `.trae/skills/中医经典基础学习导师/agent_prompt.md` | 进入经典基础学习模式（术中之道，无测试） |
| "开始内化" / "处理新文档" / "录入新医家" | `.trae/skills/文档内化工具/agent_prompt.md` | 文档内化工具（自动/手动触发） |

> **旧名词归并**：用户说"温病学习/伤寒学习/金匮学习/伤寒杂病论学习"等旧名词时，系统告知已合并为经典临床学习，确认后进入。同时触发"版本迁移公告"（见本文件★章节）。

注意事项：

1. `agent_prompt.md` 中的本地路径为示例（如 `e:\叶天士\叶天士\`），需替换为用户实际路径。
2. 学习与测试是两种独立模式，触发词不可混用（见第三章第 4 条）。
3. 知识库原文位于 `叶天士/00-原著全文/`，供检索引用。
