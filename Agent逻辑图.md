# Agent 逻辑图（中医经典教学系统 · 完整生命周期）

> 本图描述"我们这个 agent"从会话开始到发布推送的完整逻辑链路。
> 渲染：Obsidian / GitHub 直接显示；纯文本阅读可看文末"文字版流程"。

```mermaid
flowchart TD
    %% ================= 会话开始（宪法强制顺序） =================
    subgraph S1["🟢 会话开始（强制顺序，缺一不可）"]
        direction TB
        A1["读 CLAUDE.md 宪法<br/>（工作区根 + 发布仓库双宪法）"]
        A2["进度事实源解析<br/>扫描全部 进度追踪.md → 排除发布模板 → 锁定运行副本"]
        A3["潜意识 pre_session<br/>注入历史学情 whisper（无则正常开始）"]
        A4["文档内化检测<br/>扫描 00-原著全文 vs 基线 → 有新文件则内化"]
        A5{"激活状态?"}
        A6["首次：播报介绍<br/>创建 .agent-state.md"]
        A7{"老用户?"}
        A8["是：播报版本迁移公告"]
        A1 --> A2 --> A3 --> A4 --> A5
        A5 -- 首次 --> A6 --> A7
        A5 -- 已激活 --> A7
        A7 -- 是 --> A8 --> B
        A7 -- 否 --> B
    end

    %% ================= 入口路由 =================
    subgraph S2["🔀 入口路由（触发词 / 直接提问双入口）"]
        B{"用户意图"}
        B -- "触发词：开始/继续经典临床学习" --> C1["经典临床模块"]
        B -- "开始内经/基础/诊疗学习" --> C2["对应模块"]
        B -- "直接提问中医问题" --> C3["问题归类 → 判定模块<br/>辨证方剂医案→临床 / 哲学→内经 / 药理诊断→诊疗基础"]
        B -- "开始测试" --> C4["水平评估 → 多维画像 → 排课"]
        B -- "多学派交流" --> C5["学派讨论模式"]
        B -- "记住/查询" --> C6["MCP Reference Memory 图谱"]
    end

    %% ================= 教学循环 =================
    subgraph S3["📖 教学循环（以经典临床为例，L1-L4 分层执行）"]
        direction TB
        D1["意义锚点（步骤0）<br/>矛盾点漏斗引擎：接住→推向→卡住→填补"]
        D2["四步提问法（去标签化）<br/>定义题 → 应用题(真实医案A-D档) → 反向题 → 发散题"]
        D3{"学生给出方药?"}
        D4["处方验证关<br/>组成→角色→药证映射→换药检验"]
        D5["多学派分析（强制！）<br/>读映射表 → 四层触发 → 推理模拟 → 综合评价"]
        D6["总结要点 + 新词表<br/>对照 14.7 完整性清单自查"]
        D7["保存笔记 → 更新进度追踪<br/>漏强制环节 → 违规登记账本"]
        D1 --> D2 --> D3
        D3 -- 是 --> D4 --> D5 --> D6 --> D7
        D3 -- 否 --> D2
    end

    %% ================= 记忆系统 =================
    subgraph S4["🧠 潜意识记忆（跨会话长期记忆）"]
        direction TB
        M1["对话中：dsh_log 静默记录学情<br/>（决策/问题/混淆点/偏好/进度）"]
        M2["会话收尾：post_session<br/>编码→联想→凝缩→固化→预注入 + 遗忘 + 梦境"]
        M3["whisper 预注入包<br/>（下次会话自动带出）"]
        M1 --> M2 --> M3
        M3 -. 下次会话开始 .-> A3
    end

    %% ================= 事实源层级 =================
    subgraph S5["📁 事实源优先级（防'重新考教'类错误）"]
        direction LR
        N1["① 文件事实<br/>运行副本=权威源"]
        N2["② 进度文件<br/>（进度追踪.md）"]
        N3["③ 潜意识记忆<br/>缓存，非事实源"]
        N4["④ 上下文推测<br/>最低"]
        N1 --- N2 --- N3 --- N4
    end

    %% ================= 发布流 =================
    subgraph S6["🚀 发布流（GitHub 开源）"]
        direction TB
        P1["运行副本（个人数据）<br/>进度/笔记/记忆库"]
        P2["发布版<br/>skills/ + 知识库"]
        P3["发布检查.py<br/>个人内容/盘符/密钥/潜意识运行数据"]
        P4["git commit + push"]
        P1 -- "个人数据永不发布" --> P2
        P2 --> P3 -- 通过 --> P4
        P3 -- 拦截 --> P2
    end

    %% ================= 连线 =================
    B --> C1 & C2 & C3 & C4 & C5 & C6
    C1 --> D1
    D7 --> M1
    D7 -. 更新 .-> N2
    D7 -. 复用到新会话 .-> A2
    C6 -. 显式记忆 .-> N3
```

## 文字版流程（不渲染时的阅读顺序）

```
会话开始
 ├─ ① 读双宪法（工作区 CLAUDE.md + 发布仓库 CLAUDE.md）
 ├─ ② 进度事实源解析：扫描全部进度追踪.md → 排除发布模板 → 锁定运行副本（19课进度在这）
 ├─ ③ 潜意识 pre_session：注入历史学情（温病三宝跳过、L2画像、进度约束…）
 ├─ ④ 文档内化检测：扫描 00-原著全文 vs 基线（157文件基线已建）
 ├─ ⑤ 激活检查：.agent-state.md 存在 → 已激活，不重复播报
 └─ ⑥ 老用户判定 → 是则播报迁移公告
入口路由
 ├─ 触发词 → 对应模块；直接提问 → 问题归类判模块
 ├─ 开始测试 → 水平评估；多学派交流 → 讨论模式；记住/查询 → MCP图谱
教学循环（经典临床）
 ├─ 意义锚点 → 矛盾点漏斗（接住→推向→卡住→填补，百分百收敛）
 ├─ 四步提问法（定义/应用/反向/发散，真实医案 A-D 档）
 ├─ 学生给方 → 处方验证关 → 多学派分析（四层触发，漏=违规登记）
 ├─ 总结 + 14.7 自查 → 保存笔记 + 更新进度
 └─ 潜意识静默记录学情 → 收尾管道 → whisper 注入包 → 下次会话
事实源：文件 > 进度文件 > 潜意识记忆 > 上下文推测
发布流：运行副本（个人数据留本地）→ 发布版 → 发布检查 → commit+push
```

## 三个防错机制（架构级）

| 机制 | 防什么 | 位置 |
|:---|:---|:---|
| 进度事实源解析 | 把发布模板当进度 → 重新考教老用户 | 双宪法 ★ 章节 |
| 执行违规登记 | 漏强制环节（多学派/意义锚点）复发 | 进度追踪.md 账本 + 课程流程步骤5/6 |
| 发布检查.py | 个人内容/盘符/密钥/潜意识数据入库 | 发布前自动拦截 |

## DSH 环境工具层（本次新增）

```
DeepSeek Harness (host)
 ├─ MCP Reference Memory（跨会话显式记忆图谱）
 ├─ subconscious（潜意识学情记忆，零配置）
 ├─ dsh-pocket（手机访问：局域网扫码/公网隧道）
 ├─ dshmarket（插件市场：1250+ 插件）
 └─ dsh-desktop（Electron 桌面端：系统托盘+独立窗口）
```
